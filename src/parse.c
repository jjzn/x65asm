#include <stdlib.h>

#include "defs.h"

line_t parse(char* buff) {
    /* TODO: implement */

    if (buff) {} /* stop the compiler complaining */

    return (line_t) {
        NULL, PSEUDO, NULL, NULL
    };
}
